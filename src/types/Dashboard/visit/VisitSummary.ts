type VisitSummary = {
  total_visits: number | string
  unique_patients: number | string
  inpatient_visits: number | string
  outpatient_visits: number | string
  admitted_from_op: number | string
  discharges: number | string
  alos_days: number | string
  median_los_days: number | string
}